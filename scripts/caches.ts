import { AssetCache } from "@11ty/eleventy-fetch";

export const airtableCache = new AssetCache("garden.macthemes.airtable");
export const airtableFinalCache = new AssetCache(
  "garden.macthemes.airtablefinal",
);
export const kaleidoscopeCache = new AssetCache(
  "garden.macthemes.kaleidoscopeCollection",
);
